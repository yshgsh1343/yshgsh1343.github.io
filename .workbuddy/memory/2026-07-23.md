# 2026-07-23

## 博客改版（仿 zishu.me）

- 应用户要求 `git reset --hard HEAD~1` 回退了 commit 7e0aea4（调整侧边栏分界线和页面间距），回到 bd46d21。
- 侧边栏分组（仿 zishu.me）：新建 `_includes/sidebar.html` 覆盖 chirpy v7.6 主题原版，导航按 tab front matter 的 `group` 字段分组渲染「内容/站点」小标题；首页改名为「笔记」（图标 fa-pen-nib），归入内容组首位。左下角 sidebar-bottom（主题切换+联系图标）保持原样未动。
- 新增 tab：月报（order 1）、碎碎念（order 5）、留言板（order 7）；归档/分类/标签 order 调整为 2/3/4 并加 `group: 内容`，关于 order 6 `group: 站点`。月报/碎碎念页用 `site.categories["月报"]/["碎碎念"]` 列出对应分类文章，空分类有占位文案；留言板带 `comments: true`（配置了评论 provider 后自动出现评论框）。
- 页面左右边距（仿 whono）：`#main-wrapper > .container` ≥850px 时 padding 3rem，≥1200px 时 max-width 1150px，≥1650px 时 padding 4rem。
- mailto 修复：`_config.yml` 的 `social.email` 由无效的 `yshgsh1343@linuxdo` 改为 `yshgsh1343@linux.do`（用户确认）。
- 本地无 Ruby 环境无法 jekyll build；用 liquidjs（装于受管 node 工作区）对 sidebar 和三个新 tab 做了语法+模拟渲染校验，全部通过。部署走 GitHub Actions（pages-deploy.yml）。
- 本次修改已本地提交为 2a261ca。同日 17:43 用户确认 push；因回退导致本地落后远程 1 个 commit，用 `git push --force-with-lease origin main` 强推（7e0aea4...2a261ca forced update）。GitHub Actions「Build and Deploy」成功（run 29996478013），线上已验证：侧边栏分组（内容/站点）与导航项全部正确，mailto 拼出 yshgsh1343@linux.do。

## 第二轮改版（字体 + 侧栏交互，commit 17c0754，已上线）

- 字体拆分：正文/UI → 微软雅黑（"Microsoft YaHei", "PingFang SC" 等 fallback）；标题类（h1-h6、.site-title、.card-title、.dynamic-title、.post-nav-title）→ 思源宋体 Heavy（Noto Serif SC 900）。Google Fonts 改为只载 `Noto+Serif+SC:wght@700;900`。注意特异性：`html:lang(zh-CN) #sidebar`（雅黑）会压过 `.site-title`，标题组里站名选择器写成 `html:lang(zh-CN) #sidebar .site-title` 才赢。
- 侧栏右移半宽（130px）：≥1200px 时 `#sidebar { left: 130px }`、`#main-wrapper { margin-left: 390px }`；≥1650px 主题侧栏变宽 300px，对应 margin 430px、竖条 left 425px（主题变量 $sidebar-width 260 / $sidebar-width-large 300，断点 xxxl=1650）。
- 红色竖条交互：删掉 `#sidebar::after` 装饰条，改为两个 `<button class="sidebar-edge-toggle">`（放在 `</aside>` 后，fixed 定位不受侧栏 transform 影响）：10px 宽、第一条 top 25%/height 25%（1/4–1/2 处）、第二条 top 75%（底部）。点击 → body 切 `sidebar-collapsed` class → 竖条变深灰 #3d392f 并贴到屏幕左缘，侧栏 translateX(-390px) 滑出、主内容 margin-left 0；再点恢复。JS 内联在 sidebar.html 末尾（无依赖，含 aria-label/title 同步）。
- Edit 工具教训：本轮遇到 Edit 报成功但未落盘的情况，改完关键文件后务必用 Grep/Read 复查确认再提交。
- 部署：push 后 Actions 成功（run 29998199243）；线上验证 CSS（Microsoft YaHei、left:130px、sidebar-collapsed、font-weight:900）与 HTML（两个按钮+脚本、字体链接 700;900）均已生效。

## 第三轮（commit f62d220，已上线）

- 侧栏收起状态跨页面记忆：localStorage 键 `sidebar-collapsed`，点击竖条时写入；页面加载时 readState() 恢复，恢复瞬间给 `<html>` 加 `sidebar-no-anim` class（CSS 里 transition: none !important），双 rAF 后移除，避免每次打开页面都播滑出动画。localStorage 读写都包了 try/catch（隐私模式降级为会话级）。

## 环境备忘

- 本机无 ruby/bundle/jekyll；node 受管版在 `C:\Users\Lumin\.workbuddy\binaries\node\versions\22.22.2\node.exe`，npm 包装在 `C:\Users\Lumin\.workbuddy\binaries\node\workspace`。
